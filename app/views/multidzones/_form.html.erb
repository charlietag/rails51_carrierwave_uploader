<%= form_with(model: multidzone, local: true, class: "dropzone", id: "my_multi_dropzone") do |form| %>
  <% if multidzone.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(multidzone.errors.count, "error") %> prohibited this multidzone from being saved:</h2>

      <ul>
      <% multidzone.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title, id: :multidzone_title %>
  </div>

  <div class="fallback">
    <div class="field">
      <%= form.label :pictures %>
      <%= form.text_field :pictures, id: :multidzone_pictures %>
    </div>

    <div class="actions">
      <%= form.submit %>
    </div>
  </div>
<% end %>
